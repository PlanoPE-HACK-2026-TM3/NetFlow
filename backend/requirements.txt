# NetFlow Backend — Python Dependencies
# All versions verified to install together without conflicts.
# Tested on Python 3.11 and 3.12.
#
# Install: pip install -e . && pip install -r backend/requirements.txt

# ── Web framework ─────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6       # includes httptools, uvloop, websockets
sse-starlette==2.1.3            # Server-Sent Events support
starlette==0.38.6               # pinned transitively by fastapi

# ── HTTP client ───────────────────────────────────────────────
httpx==0.27.2

# ── Data validation ───────────────────────────────────────────
pydantic==2.8.2
pydantic-settings==2.5.2

# ── Environment ───────────────────────────────────────────────
python-dotenv==1.0.1

# ── LangChain stack ───────────────────────────────────────────
# IMPORTANT: langsmith 0.2.x is NOT compatible with langchain 0.3.7.
# Use 0.1.147 — last non-yanked 0.1.x release, compatible with langchain 0.3.7.
langchain==0.3.7
langchain-core==0.3.15
langchain-ollama==0.2.0
langsmith==0.1.147              # DO NOT upgrade to 0.2.x

# ── Utilities ─────────────────────────────────────────────────
tenacity==8.5.0
