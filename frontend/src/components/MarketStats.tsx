// MarketStats is now embedded inside SearchPanel to match the HTML design exactly.
// This file is kept as a no-op export for import compatibility.
export default function MarketStats(_: { zipCode?: string }) { return null; }
